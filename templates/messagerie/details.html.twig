{% extends 'base.html.twig' %}

{% block title %}Messagerie: read messages{% endblock %}

{% block body %}

<section class="mb-5">
    <div><a href="{{ path('app_messagerie_index') }}"> Retour</a></div>
    <h3 class="display-6 fw-bold text-center mb-5 mt-5 text-primary">Messages reçus</h3>

    <div class="container mb-5">
        <div class="col-md-12 pb-4">
            <div>
                <div class="row bg-white border border-muted rounded">
                    <div class="col-md-3">
                        <div>
                            <ul class="nav flex-column mx-3">
                                <li> <a href=""
                                        class="btn btn-block btn-primary text-white my-4" data-toggle="modal"
                                        data-target="#compose-modal"><i
                                            class="fa fa-pencil"></i>&nbsp;&nbsp;&nbsp;ÉCRIRE&nbsp;&nbsp;&nbsp;&nbsp;</a>
                                </li>
                                <li class="mt-3 mb-3"><a class="fs-5" href="{{ path('app_messagerie_index') }}">Messages</a></li>
                            </ul>
                        </div>
                    </div>
                    <div class="bg-light border border-muted rounded col-10 mx-auto col-lg-7 pt-4 pb-2 mt-4 mb-4">
                        <div>
                            <div class="wrapinput mb-5">
                                <label for="auteur">Expéditeur</label>
                                <input
                                    class="inputm d-block border border-muted rounded w-100 fs-5 px-4 p-3 text-primary fw-bold"
                                    id="auteur" type="text" name="expediteur"
                                    value="{{ "variableExpéditeur" }} {#<?php echo $resultat[0]['pseudo'] ?> (<?php echo $msg_nbr; ?>)#}">
                            </div>
                            <!--< ?php
                                foreach ($resultat as $result) {
                                ?>-->
                            <div class="wrapinput">
                                <input
                                    class="inputm d-block border border-muted rounded w-100 fs-6 fw-bold px-2 py-1 bg-light"
                                    type="text" name="sujet"
                                    value={{ "variable-sujet-Symfony6" }}>
                            </div>
                            <div class="wrapinput">
                                <textarea class="inputm border border-muted rounded w-100 px-4 pt-1 pb-5 fs-6 fw-bold"
                                    type="text" name="message"
                                    value="mesage">{{ "variable-dans-twig" }}</textarea>
                            </div>
                            <div class="d-flex justify-content-end">
                                <p class="pt-2 px-2 text-end time">
                                    {{ "00/00/0000" }}
                                </p>
                                <p class=" pt-2 px-2"><a class="fs-5 text-secondary text-primary"
                                        onclick="return confirm('Voulez-vous supprimer ce message?')"
                                        href="{{ path('app_messagerie_delete', {'id': message.id}) }}"><i
                                            class="fa fa-trash" aria-hidden="true"></i></a></p>

                                            <span class="btn btn-danger active w-100" onsubmit="return confirm('Êtes-vous sur de vouloir supprimer ce message ?');">{{ include('app_messagerie/_delete_form.html.twig')
                                            }}</span>


                            </div>

                            <!--< ?php
                                }
                                ?>-->
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

{% endblock %}

