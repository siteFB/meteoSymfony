{% extends 'base.html.twig' %}

{% block title %}Messagerie{% endblock %}

{% block body %}

<section>

    <h2 class="display-6 fw-bold text-center mb-5 mt-5 text-primary">Espace messagerie</h2>
    <div class="container mb-5">
        <div class="col-md-12 pb-4">
            <div>
                <div class="row bg-white border border-muted rounded px-3">

                    <div class="col-md-3">
                        <div>
                            <ul class="nav flex-column mt-5">
                                <li><a href="{{ path('app_messagerie_send') }}" class="btn btn-block btn-primary text-white my-4" data-toggle="modal" data-target="#compose-modal"><i class="fa fa-pencil"></i>&nbsp;&nbsp;&nbsp;ÉCRIRE&nbsp;&nbsp;&nbsp;&nbsp;</a></li>
                                <li class="mt-3 mb-3"><a class="fs-5" href="">Messages (<?php echo $msg_nbr; ?>)</a></li>
                            </ul>
                        </div>
                    </div>

                    <div class="col-md-9 my-5 bg-light p-3">
                        <div class="table-responsive">
                            <table class="table">
                                <tbody>

                                    <tr>
                                        {# see pseudo user & link #}
                                    {% for msg in messagerie %}     
                                        <th class="fs-5">{{ 'u.sender' }}</th>          
                                        <td class="">{{ msg.sujet  }}</td>
                                        <td class="">{{ msg.created_at|date("d/m/Y H:i:s") }}</td>
                                        <td>
                                            <a href="{# {{ path('app_messagerie_details', {'id': messagerie.id}) }}#}"
                                class="btn btn-primary w-100 active" role="button" aria-pressed="true">Voir</a>
                                        </td>
                                        <td><a class="fs-5 text-secondary" onclick="return confirm('Voulez-vous supprimer ce message?')" href="">{{ include('messagerie/_delete_form.html.twig')
                                        }}</a></td>
                                    </tr>
                                    
                                    {% else %}
                                    <tr>
                                        <td colspan="5">Aucun résultat</td>
                                    </tr>
                                    {% endfor %}
                                </tbody>
                            </table>
                        </div>
                    </div>
   
                </div>
            </div>
        </div>
    </div>
</section>

{% endblock %}
